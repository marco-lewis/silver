dj_alg(define f : {0,1} ^ 3 -> {0,1})->(define dj_ret : {0,1}^3)
pre{
    define y : {0, 1}
    define x : {0,1}^3
    define bal : {0, 1}
    define const : {0, 1}
    
    assert((¬(bal = 1)) <-> const = 1)
    assert(@x.(f(x) = 0 | f(x) = 1))
    assert($x.(x > -1))
}
post{
    assert(const = 1 -> dj_ret = 0)
    assert(bal = 1 -> ¬(dj_ret = 0))
}